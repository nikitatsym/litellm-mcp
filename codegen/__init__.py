"""Codegen inputs and tooling.

Build-time only; never imported by the runtime `litellm_mcp` package. The
inventory plus the judgment data files map, through `generate.py`, to the
committed `src/litellm_mcp/tools/_generated_*.py` tree (Decision 14).
"""
