# litellm-mcp

MCP server for the LiteLLM proxy management API.

## Development

Enable the pre-commit hook (runs `dev.py check` before every commit):

    git config core.hooksPath .githooks

Run the checks manually:

    uv run python dev.py check
